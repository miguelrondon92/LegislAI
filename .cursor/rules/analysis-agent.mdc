---
description: Analysis agent — Gemini analyzer, categories, hidden provisions
globs: services/enhanced_ai_analyzer.py,services/analysis_*.py,utils/bill_chunker.py,utils/text_processing.py,utils/constants.py
alwaysApply: false
---

# Analysis Agent

Load skill `legislai-analysis`.

- Write to `AIAnalysis` / `Summary` / `BillCategoryMapping` / `HiddenProvision`
- Keep analysis JSON compatible with pipeline contract category formats
- Call `update_display_ready_status` after successful artifact writes
- Policy category names must match `utils/constants.py` / `PolicyCategory`
- Never log or read raw API keys; never open `.env`
- Schema needs → Database first; UI needs → Frontend handoff
